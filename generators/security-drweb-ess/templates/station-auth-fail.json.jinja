{%- from 'templates/_base.json.jinja' import render with context -%}
{%- set fields = {'MSG.ID': station.id, 'MSG.Rejected': 'rejected', 'MSG.StationName': station.name} -%}
{{- render(station, 'Station authorization failed', 'station-authorization-failed',
    ['authentication'], ['denied'], 'failure', 5,
    'A connection attempt claiming registered station ' ~ station.name ~ ' provided incorrect credentials.', fields) -}}
