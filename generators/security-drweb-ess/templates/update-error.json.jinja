{%- from 'templates/_base.json.jinja' import render with context -%}
{%- set station = samples.stations.pick() -%}
{%- set fields = {
  'MSG.Product': 'Dr.Web anti-virus databases',
  'MSG.ServerTime': timestamp.isoformat()
} -%}
{{- render(station, 'Critical error of station update', 'station-update-error',
    ['package'], ['error'], 'failure', 5,
    'Anti-virus database update failed on ' ~ station.name ~ '.', fields) -}}
