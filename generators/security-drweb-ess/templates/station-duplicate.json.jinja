{%- from 'templates/_base.json.jinja' import render with context -%}
{%- set fields = {'MSG.ID': station.id, 'MSG.Server': params.server_id, 'MSG.StationName': station.name} -%}
{{- render(station, 'Station already logged in', 'station-id-duplicate',
    ['authentication'], ['info'], 'failure', 6,
    'Another connection attempt claimed the identifier of already connected station ' ~ station.name ~ '.', fields) -}}
