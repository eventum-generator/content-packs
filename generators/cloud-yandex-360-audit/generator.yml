# Yandex 360 organization audit-log event objects (one item per line).
input:
  - cron:
      expression: "* * * * * *"
      count: 1

event:
  template:
    mode: fsm
    params:
      anomaly_mode: true
      org_id: 1234567
      organization_domain: corp.example
      suspicious_ip: 198.51.100.42
      compromised_login: admin@corp.example
      compromised_uid: 1130000000123456
      sensitive_path: disk:/finance/payroll-2026.xlsx
    templates:
      - routine:
          template: templates/audit-event.json.jinja
          vars: {stage: routine}
          initial: true
          transitions:
            - to: unusual_login
              when: {defined: shared.to_unusual_login}
      - unusual_login:
          template: templates/audit-event.json.jinja
          vars: {stage: unusual_login}
          transitions:
            - to: sensitive_view
              when: {defined: shared.to_sensitive_view}
      - sensitive_view:
          template: templates/audit-event.json.jinja
          vars: {stage: sensitive_view}
          transitions:
            - to: public_link
              when: {defined: shared.to_public_link}
      - public_link:
          template: templates/audit-event.json.jinja
          vars: {stage: public_link}
          transitions:
            - to: download
              when: {defined: shared.to_download}
      - download:
          template: templates/audit-event.json.jinja
          vars: {stage: download}
          transitions:
            - to: routine
              when: {defined: shared.to_routine}

output:
  - file:
      path: output/events.json
      write_mode: overwrite
      formatter:
        format: json
