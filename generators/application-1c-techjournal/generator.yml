# 1C:Enterprise 8.3.27 technological log in its documented JSON format.
input:
  - cron:
      expression: "* * * * * *"
      count: 1

event:
  template:
    mode: fsm
    params:
      anomaly_mode: true
      anomaly_interval_events: 200
      host_name: onec-app-01
      infobase: accounting
      process_name: rphost
      routine_user: accountant01
      unusual_user: batch_admin
      routine_client_id: "8"
      unusual_client_id: "92"
    templates:
      - routine:
          template: templates/techjournal.json.jinja
          initial: true
          vars: {step: routine}
          transitions:
            - to: long_call
              when: {defined: shared.start_anomaly}
            - to: routine
              when: {always: null}
      - long_call:
          template: templates/techjournal.json.jinja
          vars: {step: long_call}
          transitions:
            - to: lock_wait
              when: {always: null}
      - lock_wait:
          template: templates/techjournal.json.jinja
          vars: {step: lock_wait}
          transitions:
            - to: lock_timeout
              when: {always: null}
      - lock_timeout:
          template: templates/techjournal.json.jinja
          vars: {step: lock_timeout}
          transitions:
            - to: exception
              when: {always: null}
      - exception:
          template: templates/techjournal.json.jinja
          vars: {step: exception}
          transitions:
            - to: routine
              when: {always: null}

output:
  - file:
      path: output/events.json
      write_mode: overwrite
      formatter:
        format: json
